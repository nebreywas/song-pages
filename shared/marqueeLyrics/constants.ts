/**
 * Marquee lyrics — single-line horizontal scroll presentation.
 * Section breaks in source lyrics (blank lines) become runs of spaces in the flat line.
 */

/** Spaces inserted between song sections (ALARE blocks) in the marquee line. */
export const MARQUEE_SECTION_GAP_SPACES = 8;

/** Horizontal inset when ALARE pacing focuses the active lyric (fraction of viewport width). */
export const MARQUEE_ALARE_LEAD_IN_RATIO = 0.12;
