/** SQLite settings key for saved VC surface designs. */
export const VC_SURFACE_DESIGNS_KEY = 'vc.surfaceDesigns';

export const VC_SURFACE_DESIGNS_VERSION = 1;

export const VC_SURFACE_DESIGN_NAME_MAX_LEN = 48;

/** At least one design must remain in the catalog. */
export const VC_SURFACE_DESIGN_MIN_COUNT = 1;
